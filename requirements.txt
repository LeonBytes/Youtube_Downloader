# YouTube Downloader Dependencies
# YouTube视频下载器依赖包

# Core dependencies - 核心依赖
yt-dlp>=2023.12.30          # YouTube下载核心库，youtube-dl的活跃分支
Pillow>=10.0.0              # 图像处理库，用于处理缩略图
requests>=2.31.0            # HTTP请求库，用于下载缩略图

# GUI dependencies - GUI依赖
# tkinter 是Python标准库的一部分，无需单独安装

# Optional but recommended - 可选但推荐
# ffmpeg - 视频格式转换工具
# 请手动安装ffmpeg到系统PATH中以获得最佳体验
# Windows: choco install ffmpeg 或从官网下载
# macOS: brew install ffmpeg  
# Linux: sudo apt install ffmpeg (Ubuntu/Debian)

# Development dependencies (可选) - 开发依赖
# pytest>=7.0.0             # 测试框架
# black>=23.0.0              # 代码格式化
# flake8>=6.0.0              # 代码检查